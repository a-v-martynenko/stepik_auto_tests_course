//div[@class="row"]/div[2] - найти 2 по счету div класса row

//p[text()="Lenin cat"] - найти p с вложенным текстом "Lenin cat"

//p[contains(text(), "cat")] - по вхождению cat в строку вложенного текста

//img[@name='bullet-cat' and @data-type='animal'] - несколько условий


Символ * — команда выбора всех элементов
Например можем найти текст в заголовке запросом //div/*[@class="jumbotron-heading"]. 
Это может быть удобно, когда мы не знаем точно тег элемента, который ищем.

Поиск по классу в XPath регистрозависим

Подойдет, когда у сайта всё плохо с атрибутами и нет возможности достучаться до разработчиков

//button[text()="Gold"]